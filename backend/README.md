# Service

Node + TypeScript service seed. Ships a `/health` endpoint and a Vitest test suite
so you can start test-first immediately.

- `npm run dev` — watch mode
- `npm test` — run tests
- `npm run typecheck` — type check
- `npm run build && npm start` — production

Add features under `src/`; keep each with a matching `*.test.ts`.
